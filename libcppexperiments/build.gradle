plugins {
  id 'cpp-library'
}

library {
  linkage = [Linkage.STATIC, Linkage.SHARED]
  baseName = "cppexperiments"
  targetMachines = [
    machines.linux.x86_64,
    machines.windows.x86, machines.windows.x86_64,
    machines.macOS.x86_64
  ]
}
